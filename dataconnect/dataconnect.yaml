specVersion: "v1beta"
serviceId: "netflix_gpt"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "netflix_gpt-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
